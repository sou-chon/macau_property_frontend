import React, { FunctionComponent as FC } from 'react';
import { NavLink } from 'react-router-dom';
import style from './scaffold.module.css';

export const MenuBar: FC = () => {
    return (
        <div className={style.main_menubar}>
            just a menu
        </div>
    );
};
